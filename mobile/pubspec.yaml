name: sabjiwala5_mobile
description: "A production-grade hyperlocal mobile application for Sabjiwala 5 customers, vendors, and delivery partners."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  google_maps_flutter: ^2.5.3
  geolocator: ^10.1.0
  socket_io_client: ^2.0.3+1
  http: ^1.2.0
  firebase_core: ^2.27.0
  firebase_messaging: ^14.7.15

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
