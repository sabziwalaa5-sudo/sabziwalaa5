# Copy to web/.env.local — never commit real secrets.

# Copy to web/.env.local — never commit real secrets.

NEXT_PUBLIC_APP_URL=https://web-sabziwalaa5.vercel.app
# Optional separate admin host. Defaults to {APP_URL}/admin on the same Vercel site.
NEXT_PUBLIC_ADMIN_URL=https://web-sabziwalaa5.vercel.app/admin
NEXT_PUBLIC_SUPABASE_URL=https://qbcchkhjbrijrqubzvtk.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_zsvJF9EmiuER-zpMuHd7gg_c9MEY-ql
# Legacy name still accepted if set:
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only. Do not prefix with NEXT_PUBLIC.
PAYMENT_HMAC_SECRET=
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
RAZORPAY_WEBHOOK_SECRET=

# Staff portals work without Supabase when this PIN is used.
# Default in code is Sabjiwala5! — override in Vercel Config (not Secret + NEXT_PUBLIC).
STAFF_BOOTSTRAP_PASSWORD=
STAFF_SESSION_SECRET=

# Optional: point the native WebView at a non-production host
# CAPACITOR_SERVER_URL=https://web-sabziwalaa5.vercel.app
